# SocialAiBlog

a futuristic BLOG Frontend UI with FastAPI backend main.py is already setup lets cook the UI to broadcast like CNN 2045 + ChatGPT 5.2 + Social Media platform feeling

## Getting Started

```bash
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## API Docs

Visit `/docs` for interactive API documentation.
