# Discord Bot

Lets make a discord bot with OpenAI spec AI Agents using remote baseURL for compatibility and each channel/topic is a workspace with contextual memory

## Getting Started

```bash
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## API Docs

Visit `/docs` for interactive API documentation.
